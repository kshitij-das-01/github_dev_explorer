GitHub Developer Explorer Multi-Endpoint APIs
A tool utilizing the public GitHub REST API. Users search a username to view their profile, stats, and a sortable list of repositories. Requires combining multiple endpoints, handling the 60 req/ hour unauthenticated rate limit gracefully, and data transformation (e.g., computing a language breakdown chart). 
